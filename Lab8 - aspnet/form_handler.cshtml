@{ 
    if (Session["rules"] == null)
    {
        Session["rules"] = new List<String>();
    }

    var rules = (List<String>)Session["rules"];
    if (IsPost && Request["rule"] != null)
    {
        rules.Add(Request["rule"]);
    }

    Response.Redirect("Rules.cshtml");
}